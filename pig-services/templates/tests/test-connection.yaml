apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pig-services.fullname" . }}-test-connection"
  labels:
    {{- include "pig-services.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pig-services.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
