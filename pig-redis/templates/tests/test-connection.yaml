apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pig-redis.fullname" . }}-test-connection"
  labels:
    {{- include "pig-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pig-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
